# DESIGN_DECISIONS.md（案）

## 1. コンセプト

住所を入力すると、危険エリア（内水・外水の浸水想定0.5m以上）を迂回しながら、最寄りの一時避難場所（高地）への徒歩ルートを提示するツール。

**位置づけ**：実際の浸水レベルは発生してみないとわからないため、本ツールはリアルタイムの避難誘導ではなく、**事前に避難コースを検討するための参考材料**として提供する。

---

## 2. スコープ

### 対象とするもの
- 対象地域：住所（自宅・勤務先など複数登録可）＋隣接市区町村まで。全ての隣接市町村を含める必要はなく、近い市区町村のみの選択でも可（例：市川市＋松戸市）
- 移動手段：徒歩のみ
- 避難先：指定緊急避難場所（一時避難）。指定避難所（生活避難所）は対象外
- 避難所候補：最寄り1件だけでなく、第3候補まで提示

### スコープ外（明示的に対象としないもの）
- 車での避難：浸水後は身動きが取れなくなるリスクの方が大きく、複雑化も避けたいため対象外とする。車を使いたい場合は、本ツールの参照結果をもとにより早く行動することを促す
- 県またぎ等の広域対応・全国一律対応：「居住地＋隣接市区町村」に留める
- 要配慮者（高齢者・車椅子利用者等）向けの個別対応：身体状況・介助者の有無・移動速度等によって必要な避難経路が大きく異なるため、本ツールでは個別最適化を行わない
- 避難所の定員・混雑状況などのリアルタイム情報
- 家屋倒壊等氾濫想定区域：v1では個別レイヤーとしてルーティング判定に使用しない。ただし、浸水深だけでは表現できない危険性（流速・河岸侵食等）を含むため、将来的な拡張対象とする

---

## 3. データ方針

### 外水（河川氾濫浸水想定区域）
- 国土交通省（重ねるハザードマップ等）の全国データを使用
- 市町村ごとの個別データ・利用規約確認は行わない（国・県データを使うことで確認コストを下げる）
- ユーザーには設定させず、アプリ側に固定値として組み込む

### 内水（雨水出水浸水想定区域）
- 市町村ごとのハザードマップに依存（多くはPDF形式で、GIS形式配布は稀。稀にWeb地図形式もあるが、ゼンリン等の地図ベンダーにデータ著作権が帰属し「閲覧・印刷のみ許諾、複製・抽出・加工は不可」というケースが多い）
- 各自治体の内水データは、以下の三値でステータス管理する。**ホワイトリストは「使ってよい自治体一覧」ではなく、「二次利用可否について所定の確認を完了した自治体一覧」と定義する**

| ステータス | 定義 | 該当条件 |
|---|---|---|
| ホワイト | 二次利用可能と確認できた | CC BY等のライセンスが明記されている、オープンデータカタログに明示的に含まれている、等 |
| グレー | 未確認／条件が読み取れない | オープンデータカタログに掲載がない、規約が曖昧、ベンダー系プラットフォーム（ゼンリン等）経由で要精査、等。**「カタログに無い＝著作権上利用不可」ではなく、あくまで「このプロジェクトとして二次利用可能と確認できていない」という運用上の保守的な扱い** |
| ブラック | 二次利用不可と明記されている | 規約に「複製・抽出・加工・転載等を禁止する」旨が明記されている場合 |

- エンドユーザーへの表示は、グレー・ブラックいずれも「データ無し」として、元のセル色を維持したまま半透明の斜線を重ねて表示する（安全区域との誤認防止）。ただしブラックの場合のみ、「権利関係により表示できません。公式サイトでご確認ください」の文言と公式URLへのリンクを追加する
- 一次スクリーニングとして、リンク先URLがベンダー系プラットフォーム特有のパターン（例：`agree.html?lay=...`形式の同意画面を経由する等）に該当するか、規約文中に地図ベンダー名（ゼンリン等）が含まれるかを先に確認し、該当すればブラック候補として詳細確認を省略してよい
- 自治体追加のたびに、上記三値のいずれに該当するかを判定し、DATA_SOURCES.mdに記録する運用とする

### 高潮浸水想定区域
- 沿岸部のみ該当。内陸地は境界が不明瞭なため「対象外」として扱う（安全とはみなさない）
- 参考情報として扱い、実際の危険度判断は住民の土地勘に委ねる

### 浸水想定の優先順位・重複時の扱い
- 想定最大規模 ＞ 計画規模の優先度。データが無い場合は存在するものを採用
- 複数レイヤーが重複する区間は、最も深度が大きい値を採用
- 市町村境界での浸水想定の不連続は許容する（対応しない）

---

## 4. 危険エリア判定・ルーティングロジック

- 「河川沿い」の判定は、距離バッファ方式ではなく**外水浸水想定区域データそのもの**で行う
- 通常時：0.5m以上の危険エリア（内水・外水とも）をすべて回避したグラフ上で、安全に到達できる避難所のみを距離順に第3候補まで提示する。**安全な避難所が1件でも見つかった場合、危険エリアを経由する避難所は候補として提示しない**（安全ルートと危険ルートを混在させない、オール・オア・ナッシング方式）
- 危険エリアに完全に囲まれる場合（＝安全に到達できる避難所が1件もない場合）のみ、フォールバックへ移行する：河川沿い（外水由来）は絶対に除外した上で、内水は通行可能とし、経路上の最大深度が最も浅くなるルートを候補とする。この場合、そのルートに含まれる浸水想定の**深度範囲**（例：0.5〜0.8m）を提示し、実際に通過するかどうかの判断は利用者に委ねる（「危険エリア・速やかな通過が必要」である旨も併記する）
- 外水由来の危険エリアは、深度によらず常に絶対除外とする（水深1m超の外水は流速を伴い特に危険性が高いことが根拠の一つだが、v1では深度で区別せず外水は一律絶対除外とする）
- 危険エリア判定の粒度：道路がハザードポリゴンにわずかに掠る程度では「危険」とみなさない（早期避難用ルートのため）。半分程度被る場合は、通行困難・足止めリスクが高いとして危険エリア側に判定する
- 土砂災害リスクとの交差：避難所候補が土砂災害リスクと重なる場合は、注釈として記載し選択の補助とする
- **0.5mという閾値の位置づけ**：「歩いても安全な水深」の基準ではなく、浸水がこの水準に達すると避難行動そのものが困難になる可能性があるため、早期のルート計画段階で回避対象とするための設計上の閾値である（水が流れていることで、静止した水よりも足を取られやすい点も踏まえている）

---

## 5. UI・運用方針

- ユーザーが入力するのは住所のみ。市区町村コード、データURL、ファイルパス等は一切入力させない
- ファイルの格納先はアプリ側が管理し、ユーザーに任せない
- 住所データはローカル保存とし、サーバーには送信しない
- 所要時間の表示は「参考」扱いとし、「実際にはより時間がかかる可能性があります」の注記を必須とする（浸水0.5m未満でも歩行に遅れが生じるため）
- 0.5mという閾値の根拠について、一行の補足説明を添える（水が流れていることで足を取られる点を含める）
- オフライン利用対策として、ルートの印刷・画像保存機能を用意する
- データ更新は、ローカルへのダウンロードまたは参照URLの指定方式で対応し、完全自動追従は目指さない
- 避難所データの更新頻度は、事前確認用途である以上、大きな課題とはしない

---

## 6. ライセンス・ドキュメント運用

- 外水は国・県データを使うことで利用規約確認の手間を削減
- 内水は自治体ごとに利用規約（CC BY 4.0相当、総務省ガイドライン準拠表明の有無等）を確認し、上記のホワイト／グレー／ブラックいずれに該当するかを判定してDATA_SOURCES.mdに記録する運用とする
- 大半の自治体は明示的な許諾表示がなく「グレー」が既定状態になる見込みのため、ホワイトリストの拡充は緩やかなペースになることを前提としておく

### リポジトリ構成（圧縮版）

| 文書 | 扱い |
|---|---|
| README.md | 必須（冒頭に免責事項・プライバシー方針を統合） |
| LICENSE | 必須（コードのライセンス。データのライセンスとは分離） |
| DATA_SOURCES.md | 必須（自治体ごとの出典・ライセンス・登録日の台帳） |
| ADDING_MUNICIPALITY.md | 必須（自治体追加の手順・判断基準のチェックリスト） |
| DESIGN_DECISIONS.md | 必須（本ドキュメント。最初は薄くてよい） |
| CONTRIBUTING.md | 外部からの参加が始まってから整備 |
| Issue/PRテンプレート | 自治体追加の受付を本当に開始する段階で整備 |

---

## 7. 懸案事項（未決定・要検討）

- 内水ハザードマップの色凡例が自治体ごとに異なる可能性があり、浸水ランクのデコードロジックを自治体ごとに個別実装する必要がありそうな点
- 経路探索の実行環境（クライアントサイド完結か、サーバーサイドか）が未決定
- 家屋倒壊等氾濫想定区域・土砂災害警戒区域データの入手方法や形式についての詳細調査は未着手
- 「安全な避難所は大回りだが、近距離に浅い内水（1m未満）を経由すれば早く着く避難所がある」ケースについて、現状は安全ルートのみを候補とし危険側は一切提示しない方式としているが、この扱いで妥当かは実例を見ながら継続検討する
