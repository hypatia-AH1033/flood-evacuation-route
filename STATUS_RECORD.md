# Status: Research / Prototype

本リポジトリは、内水浸水情報を利用した避難経路安全性判定の実現可能性を検討するための資料・試作です。自治体公開データの形式、属性定義、二次利用条件を確認した結果、現時点では実データを用いた一般化可能な検証には至っていません。

以下は検討の記録です。

---

## 現在の設計

- 住所を入力すると、危険エリア(内水・外水の浸水想定0.5m以上)を回避した最寄り避難所(高地)へのルートを提示する
- 対象範囲は居住地＋隣接市区町村までとし、広域対応は行わない
- 移動手段は徒歩前提
- 危険エリアに完全に囲まれる場合は、フォールバックとして最も浅い区域を通るルートを提示し、深度範囲を明示した上で通過判断は利用者に委ねる
- 外水データは国交省(MLIT)の全国データで代替可能な場合はそれを使用し、市町村ごとの利用規約確認の手間を減らす方針
- 内水データについては自治体ごとに二次利用可否を確認し、AVAILABLE(確認済み) / NONE(データ無し) / GRAY(未確認) / BLACK(二次利用不可) の4状態で管理する
- 外水がAVAILABLE以外の場合はシステムとしてシミュレーションを開始しない(絶対除外条件の前提のため)。内水がAVAILABLE以外でも外水のみでシミュレーションは成立する

## 想定していたデータモデル

自治体の内水浸水データは、浸水深が連続値ではなく「0.3–0.5m」「0.5–1.0m」等の階級(レンジ)として提供されることを前提としていた。この前提に基づき、判定ロジックの期待値も「深度階級(レンジ)としての認識」を軸に設計していた。

## ケース①の試験仕様

実データ検証の最初のケースとして、以下を確認対象とした。

- 目的: 既知の浸水想定区域内(0.5m以上)の座標が正しく「危険エリア」と判定されることの疎通確認
- 事前期待値: ①当該座標が0.5m以上の深度階級として認識されること(is_dangerous = true) ②対応する深度階級(レンジ)を取得できること
- PASS条件: 上記①②がいずれも満たされること
- 単一の数値depthを要求しない設計とした(想定データモデルに合わせたもの)

## 松戸市で実データを確認した結果

千葉県オープンデータサイト経由で入手した松戸市の内水ハザードマップ実データ(Shapefile形式、ポリゴン、9,969フィーチャ、JGD2011平面直角座標系第9系)を確認した。

属性テーブルの構成:

| フィールド | 内容 |
|---|---|
| ID | 一意ID |
| DEPTH | 浸水深(連続値・小数) |
| RANK | 階級コード(1〜5の整数) |

## DEPTH / RANK の実態

想定データモデルとの不一致が確認された。DEPTHは連続値であり、「階級のみで提供される」という前提と一致しなかった。

RANKは整数コード(1〜5)として存在するが、付属のメタデータ(xml)にRANK値と深度レンジの対応関係を示す公式な凡例・ラベルは含まれていなかった。データ内のDEPTH最小・最大値をRANKごとに集計すると以下の分布が見られたが、これはあくまで実データからの観測結果であり、公式な階級定義ではない。

| RANK | DEPTH範囲(観測値) | 件数 |
|---|---|---|
| 1 | 0.05〜0.30m | 8,192 |
| 2 | 0.30〜0.50m | 1,258 |
| 3 | 0.50〜1.00m | 438 |
| 4 | 1.00〜2.92m | 79 |
| 5 | 3.15〜3.84m | 2 |

RANKの意味を観測値からの推測ではなく公式な定義として採用してよいかどうかを判断できなかった。

## 静岡市を調査した結果

別自治体の例として静岡市を調査した。水害(洪水・内水・高潮)ハザードマップは公式サイト上で存在が確認できたが、提供形態は冊子配布・PDF(情報・学習面のみ)・電子書籍・WebGIS(「しずマップ」)の4種類にとどまり、shp/GeoJSON等の機械可読な一次データへの直接ダウンロードリンクは確認できなかった。

サイト全体の著作権規定は存在するが、ハザードマップという個別データセットの二次利用条件を明記したものではなかった。オープンデータについては別サイト(オープンデータカタログサイト)が用意されているが、その利用規約は当該サイト側に委ねられており、ハザードマップデータがそこに含まれているかどうかは今回の調査範囲では確認できなかった。

## データ形式・属性定義・利用許諾がボトルネックになったこと

今回の検証で共通して直面したのは、以下の3点である。

1. **データ形式**: 機械可読な一次データ(shp等)が、そもそも公開されていない、あるいは見つけにくい自治体がある
2. **属性定義**: 一次データが存在しても、属性コード(例: RANK)の意味を示す公式な凡例・ドキュメントが付属していない場合がある
3. **利用許諾**: サイト全体の一般的な著作権表記はあっても、個別データセットの二次利用条件を明記したものは少ない

## 現時点では実データによるPASS/FAIL判定を保留

上記の理由により、ケース①のPASS/FAIL判定は現時点では保留とする。特定自治体のデータを用いた実用検証よりも、公開データを安全かつ再利用可能な形で取り込むための条件整理が先行する段階と判断している。

これらの条件が明確になった機械可読データが今後広くオープンデータとして提供されれば、自治体ごとの浸水情報を取り込んだ経路安全性判定への応用が期待できる。
